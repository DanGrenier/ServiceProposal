<!DOCTYPE html>
<html>
<head>
  <title>Proposals</title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-sacle=1">
  <%= include_gon(:init => true) %>
  <%= stylesheet_link_tag    'application', media: 'all'%>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
</head>
<body class="<%= controller_name%> <%= action_name%>">
  <nav class = "navbar navbar-default navbar-statis-top" role="navigation">
    <div class ="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-nav-collapse" aria-expanded="false">	
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <!-- Home Button with Logo -->
        <%= link_to root_path, class:"navbar-brand" do %>
          <%=image_tag("LogoIcon.png", alt:"Padgett") %>
        <%end%>
      </div>
      <%= render "shared/menubar"%>
    </div>
  </nav>    
  
  <div class = "container">
  	<div class = "row">
  	  <%flash.each do |name,msg|%>
  	    <!-- Get rid of the timedout flash message since it was showing up twice on timeouts  -->
  		<%unless name.to_s == 'timedout'%>
  		  <div class = 'alert alert-<%="#{name}" %>'>
  		    <a href ='#' class='close' data-dismiss='alert'> &#215;</a>
            <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
          </div>  
        <%end%>
      <%end%>
      <%flash.discard()%>  
    </div>
    <%= yield %>  
  </div>
</body>
</html>
 

